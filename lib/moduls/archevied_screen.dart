import 'package:flutter/material.dart';

class ArcheviedScreen extends StatelessWidget {
  const ArcheviedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Archevied Tasks'));
  }
}
